# ~/.ssh/config — managed by github.com/josecolella/dotfiles
# Colima container-runtime config; missing includes are silently ignored by ssh.
Include ~/.colima/ssh_config

Host *
  AddKeysToAgent yes
  UseKeychain yes
  IdentityFile ~/.ssh/id_ed25519
